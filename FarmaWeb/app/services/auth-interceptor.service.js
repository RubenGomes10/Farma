(function () {
    'use strict';

    angular.module('FarmaciaApp').factory('authInterceptorService', authInterceptorService);

    authInterceptorService.$inject = ['$http', '$rootScope'];
    function authInterceptorService($http, $rootScope) {


    }

})();